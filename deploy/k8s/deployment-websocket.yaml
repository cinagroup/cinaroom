apiVersion: apps/v1
kind: Deployment
metadata:
  name: cinaseek-websocket
  namespace: cinaseek
  labels:
    app: cinaseek
    component: websocket
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cinaseek
      component: websocket
  template:
    metadata:
      labels:
        app: cinaseek
        component: websocket
    spec:
      containers:
      - name: websocket
        image: cinagroup/cinaseek-ws:latest
        imagePullPolicy: Always
        command: ["/cinaseek-ws"]
        workingDir: "/app"
        ports:
        - containerPort: 8081
          name: ws
        envFrom:
        - configMapRef:
            name: cinaseek-config
        - secretRef:
            name: cinaseek-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: logs
        emptyDir: {}
